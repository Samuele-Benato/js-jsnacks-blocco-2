// chiedo un numero all'utente
let number = prompt("inserisci un numero");
// se il numero è pari lo stampo
if (number % 2 == 0) {
} else {
  // altrimenti ne aggiungo un numero
  let = number++;
  number = number++;
}
